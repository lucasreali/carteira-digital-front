# A base URL da API é fixa no código (src/api/config.ts): o mock server do
# contrato publicado no Apidog.

# Enquanto "true", as requisições são atendidas por um mock em memória com os
# dados de exemplo da documentação, sem sair do navegador.
VITE_ENABLE_MOCK_API=false
